//Fibonacci Series using Recursion
#include<stdio.h>

int fib(int n)
{
   if (n <= 1)
      return n;
   return fib(n-1) + fib(n-2);
}

int entrance (int argc, char** argv)
{
  int n = 9;
  printf("%d", fib(n));
  getchar();
  return 0;
}